import React from "react";
import { PreviewPageContainer } from "@devgateway/dvz-ui-react/layout";

const PreviewPageRoute = ({ params }: any) => {
  // const { lan, id } = params;
  return <PreviewPageContainer />;
};

export default PreviewPageRoute;