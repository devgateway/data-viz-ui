import React from "react";
import { PreviewTypeContainer } from "@devgateway/dvz-ui-react/layout";

const PreviewTypeRoute = ({ params }: any) => {
  return <PreviewTypeContainer />;
};

export default PreviewTypeRoute;