import React from "react";
import { SlugContainer } from "@devgateway/dvz-ui-react/layout";

const SlugRoute = ({ params }: any) => {
  // const { lan, slug, parent } = params;
  return <SlugContainer />;
};

export default SlugRoute;